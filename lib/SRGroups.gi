#
# SRGroups: Self-replicating groups of regular rooted trees.
#
# Implementations
#
InstallGlobalFunction( SRGroups_Example,
function()
	Print( "This is a placeholder function, replace it with your own code.\n" );
end );

